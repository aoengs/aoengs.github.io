---
// Astro frontmatter
---

<html lang="zh-CN">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>小明的空间</title>
    <style>
      /* 添加 Safari 兼容性样式 */
      :root {
        --grid-gap: 1rem;
        --card-padding: 1.5rem;
      }

      body {
        margin: 0;
        background: #f5f5f5;
        font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
        -webkit-font-smoothing: antialiased;
        -moz-osx-font-smoothing: grayscale;
      }

      .container {
        max-width: 1200px;
        margin: 0 auto;
        padding: 2rem;
        -webkit-perspective: 1000px;
        perspective: 1000px;
      }

      .grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
        gap: var(--grid-gap);
        -webkit-transform-style: preserve-3d;
        transform-style: preserve-3d;
      }

      .card {
        background: #fff;
        border: 1px solid rgba(0,0,0,0.1);
        padding: var(--card-padding);
        border-radius: 12px;
        position: relative;
        overflow: hidden;
        will-change: transform;
        transition: transform 0.3s ease, box-shadow 0.3s ease;
      }

      /* 添加卡片悬浮效果 */
      .card:hover {
        box-shadow: 0 10px 20px rgba(0, 0, 0, 0.08);
      }

      /* 优化文字渲染 */
      .card h1, .card h2, .card p {
        -webkit-transform: translateZ(0);
        transform: translateZ(0);
        -webkit-font-smoothing: antialiased;
        -moz-osx-font-smoothing: grayscale;
        /* 防止文字抖动 */
        backface-visibility: hidden;
        transform-style: preserve-3d;
      }

      /* 添加卡片内容容器 */
      .card-content {
        position: relative;
        z-index: 1;
        transform: translateZ(0);
      }

      .card-large {
        grid-column: span 2;
        grid-row: span 2;
      }

      .card-medium {
        grid-column: span 2;
      }

      .intro-title {
        font-size: 2.5rem;
        margin: 0 0 1rem 0;
        font-weight: 600;
      }

      .intro-subtitle {
        font-size: 1.2rem;
        color: #666;
        margin: 0;
        line-height: 1.6;
      }

      .project-title {
        font-size: 1.5rem;
        margin: 0 0 0.5rem 0;
      }

      .project-desc {
        color: #666;
        margin: 0;
        line-height: 1.6;
      }

      .card-link {
        text-decoration: none;
        color: inherit;
        display: contents;
      }

      /* 头像容器样式 */
      .avatar-container {
        display: flex;
        align-items: center;
        margin-bottom: 20px;
      }

      /* 头像样式 */
      .avatar {
        width: 80px;
        height: 80px;
        border-radius: 50%;
        border: 3px solid #fff; /* 白色内边框 */
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        object-fit: cover;
        margin-right: 20px;
        transition: transform 0.3s ease, box-shadow 0.3s ease;
      }

      /* 头像悬浮效果 */
      .avatar:hover {
        transform: scale(1.05);
        box-shadow: 0 0 0 4px #0f33bf; /* 悬浮时边框颜色变浅 */
      }

      /* 文字区域样式 */
      .intro-text {
        flex: 1;
      }

      .intro-title {
        margin: 0;
        font-size: 1.8rem;
        font-weight: 600;
      }

      .intro-subtitle {
        margin: 8px 0 0 0;
        color: #4a5568;
        font-size: 1.1rem;
      }
    </style>
    <script type="module">
      import { animate, spring } from 'https://cdn.skypack.dev/@motionone/dom';

      document.addEventListener('DOMContentLoaded', () => {
        const cards = document.querySelectorAll('.card');
        
        cards.forEach(card => {
          // 悬浮动画效果
          card.addEventListener('mouseenter', () => {
            animate(card, {
              scale: 1.02,
              y: -5,
              boxShadow: '0 20px 40px rgba(0, 0, 0, 0.08)'
            }, {
              duration: 0.3,
              easing: spring({ stiffness: 300, damping: 20 })
            });
          });

          // 移走时立即恢复原状
          card.addEventListener('mouseleave', () => {
            animate(card, {
              scale: 1,
              y: 0,
              rotateX: 0,  // 确保旋转角度归零
              rotateY: 0,  // 确保旋转角度归零
              boxShadow: '0 5px 15px rgba(0, 0, 0, 0.05)'
            }, {
              duration: 0.3,
              easing: spring({ stiffness: 400, damping: 30 }) // 增加弹簧刚度和阻尼
            });
          });

          // 鼠标移动时的倾斜效果
          card.addEventListener('mousemove', (e) => {
            const rect = card.getBoundingClientRect();
            const x = e.clientX - rect.left;
            const y = e.clientY - rect.top;
            
            const centerX = rect.width / 2;
            const centerY = rect.height / 2;
            
            const rotateX = (y - centerY) / 20;
            const rotateY = (centerX - x) / 20;

            animate(card, {
              rotateX,
              rotateY,
            }, { 
              duration: 0.1,
              easing: [0.2, 0.2, 0.2, 1]
            });
          });
        });

        // 导航卡片特殊处理
        const navigationCard = document.querySelector('.card[data-type="navigation"]');
        if (navigationCard) {
          navigationCard.addEventListener('click', (e) => {
            e.preventDefault();
            window.location.href = '/navigation';
          });
        }
      });
    </script>
  </head>
  <body>
    <div class="container">
      <div class="grid" id="grid">
        <!-- 个人介绍卡片 -->
        <div class="card card-large">
          <div class="avatar-container">
            <img 
              src="/path/to/your/avatar.jpg" 
              alt="头像" 
              class="avatar"
              onError="this.src='https://api.dicebear.com/9.x/adventurer-neutral/svg?seed=Felix'" <!-- 使用默认头像作为备选 -->
            <div class="intro-text">
              <h1 class="intro-title">Hi,I'm Bailey</h1>
              <p class="intro-subtitle">一名热爱创造的前端开发者</p>
            </div>
          </div>
          <p class="intro-desc">专注于打造美好的用户体验，热爱技术与创新</p>
        </div>

        <!-- 导航卡片 -->
        <a href="/navigation" class="card-link">
          <div class="card card-medium" data-type="navigation">
            <h2 class="project-title">工具集合</h2>
            <p class="project-desc">互联网应用导航栏，集合了 Github，AI 工具，设计工具等等</p>
          </div>
        </a>

        <!-- 项目卡片 -->
        <div class="card card-medium">
          <h2 class="project-title">技能</h2>
          <p class="project-desc">JavaScript, React, Vue, Node.js</p>
        </div>

        <!-- 博客卡片 -->
        <div class="card card-medium">
          <h2 class="project-title">最新博客</h2>
          <p class="project-desc">如何使用 Motion 制作流畅的网页动画</p>
        </div>

        <!-- 音乐卡片 -->
        <div class="card">
          <h2 class="project-title">最近在听</h2>
          <p class="project-desc">Electronic & Dance Music</p>
        </div>

        <!-- 联系方式卡片 -->
        <div class="card">
          <h2 class="project-title">联系我</h2>
          <p class="project-desc">GitHub · Twitter · Email</p>
        </div>
      </div>
    </div>
  </body>
</html>